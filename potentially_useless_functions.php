<?php
	// je-desto-beziehung #1
	je ($ausloeser) {
		# code...
	} desto($resultat) {

	}

	// je-desto-beziehung #2
	foreach ($ausloeser as $je => $desto) {
		# code...
	}

	//how to do offscreen-rendering like a pro
	if ( offscreen() ) {
		//do jack shit.
	}	

	public static function MakeProfitFromUnderpants() {
		if ( underpants() ) {
			foreach ($clothing as $underpants => $profit) {
				//magic happens here...
			}
		} else {
			do($nothing);
		}
	};

?>